# Contributing

1. Fork the repository and create a feature branch.
2. Install dependencies with `pnpm install`.
3. Run `pnpm build` before opening a pull request.
4. Use the release workflow for Windows and macOS packaging.
